<section class="section">
  <div class="wrap wrap--narrow">
    <h1 class="section-head">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Página", "settings": [] }
{% endschema %}
