<section class="section">
  <div class="wrap wrap--narrow empty-state stack">
    <h1>{{ 'not_found.title' | t }}</h1>
    <p class="lede">{{ 'not_found.text' | t }}</p>
    <div><a class="btn" href="{{ routes.root_url }}">{{ 'not_found.action' | t }}</a></div>
  </div>
</section>

{% schema %}
{ "name": "Página 404", "settings": [] }
{% endschema %}
